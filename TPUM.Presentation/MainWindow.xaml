<Window x:Class="TPUM.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TPUM.Presentation" 
        xmlns:vm="clr-namespace:TPUM.Presentation.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <vm:MainViewModel x:Key="MainViewModel"/>
    </Window.Resources>
    <Grid DataContext="{StaticResource MainViewModel}">
        <Button Content="Button" HorizontalAlignment="Left" Margin="212,108,0,0" VerticalAlignment="Top" Height="53" Width="128" Command="{Binding DoCommand}"/>
        <Button Content="Button2" HorizontalAlignment="Left" Margin="400,171,0,0" VerticalAlignment="Top" Height="83" Width="127" Command="{Binding DoNextCommand}"/>
    </Grid>
</Window>
